sudo apt update && sudo apt install -y chromium

# 安装 Chromium 及必要依赖
sudo apt update
sudo apt install -y chromium fonts-liberation libappindicator3-1 libnss3 lsb-release xdg-utils

# 可选：安装无头模式依赖（如果截图需要）
sudo apt install -y xvfb

# x11 必选
sudo apt install -y xauth xorg

# Chrome 在 Linux 上显示中文方块，是字体缺失问题。直接修复：
1. 安装中文字体
Debian/Ubuntu:
sudo apt update && sudo apt install fonts-wqy-zenhei fonts-wqy-microhei fonts-noto-cjk

# 自动化
```bash
npm install -g playwright
npx playwright install chromium
```

# 启动
chromium --headless=new --remote-debugging-port=9222 --no-sandbox --disable-gpu --user-data-dir=/tmp/chromium-headless 

# 